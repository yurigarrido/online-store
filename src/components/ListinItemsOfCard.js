/* import React from 'react';

class ListinItemsOfCard extends React.Component {
  render() {
    const { addedToCar } = this.props;
    console.log(addedToCar);
    return (
      <div>
        {addedToCar.map((iten) => (
          <div key={ iten.title }>
            <p data-testid="shopping-cart-product-name">
              { iten.title }
            </p>
            <p>
              { iten.price }
            </p>
             <p data-testid="shopping-cart-product-quantity">Quantidade</p>
          </div>
        ))}
      </div>
    );
  }
}

export default ListinItemsOfCard; */
